# One Screen AI Tool

Next.js（App Router）+ TypeScript で構成した最小構成のプロジェクトです。

## ローカル起動方法

```bash
npm install
npm run dev
```

`http://localhost:3000` にアクセスしてください。

## ビルド方法

```bash
npm run build
npm run start
```

## Lint

```bash
npm run lint
```
